name: "Compilación del documento .tex en la carpeta de informes"

on:
  push:
    branches:
      - main

jobs:
  release_cv:
    name: "Ejecución Informe LaTeX"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout del repositorio"
        uses: actions/checkout@v1

      - name: "Compilación del Informe"
        uses: xu-cheng/latex-action@master
        with:
          root_file: Informes/Informe01.tex

      - name: "Revisión del PDF"
        run: |
          file Informes/Informe01.pdf | grep -q 'PDF'

      - name: "Carga del PDF como release"
        uses: marvinpinto/action-automatic-releases@latest
        with:
          repo_token: ${{ secrets.TOKEN_ACTION }}
          automatic_release_tag: "latest"
          prerelease: true
          title: "Carga del informe"
          files: |
            Informe01.pdf
